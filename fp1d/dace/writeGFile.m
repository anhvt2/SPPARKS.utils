% Oct 7, 2017
% Anh Tran
% MATLAB translation of writeGFile.py in Python
% incomplete; but no need to be complete because Python v3.6 is installed

function [p, k_range, j_range] = BezierSurfaceSupVec(cpp, u, v)
	k_range = length(cpp());
end
